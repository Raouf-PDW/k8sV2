apiVersion: apps/v1
kind: Development
metadata:
  name: mongodb-development
  labels:
    app:  mongodb
spec: 
  replicas: 1
  selector: 
    matchlabels:
      app:  mongodb
  template:
    metadata:
      labels: 
        app:  mongodb
    spec: 
      containers:
      - name: mongodb
        image:  mongo
        ports:
        - containerPort:  27017
        env:  
        - name: MONGO_INITDB_ROOT_USERNAME
          value:  
        - paasowrd: MONGO_INITDB_ROOT_PASSWORD
          value:  

